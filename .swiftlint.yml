# SwiftLint default configuration

# Paths to ignore
excluded:
  - Pods
  - Carthage
  - .build

disabled_rules:
  - function_body_length
  - type_body_length
  - type_name
  - line_length
  - file_length
  - identifier_name
  - todo

reporter: "github-actions-logging"
strict: true
